import { SlideOnHoverDirective } from './slide-on-hover.directive';

describe('SlideOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
